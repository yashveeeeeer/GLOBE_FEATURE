/// <reference types="vite/client" />

/**
 * Global constant injected by Vite `define` – points to the base URL
 * where CesiumJS static assets (Workers, Assets, ThirdParty, Widgets) are served.
 */
declare const CESIUM_BASE_URL: string;
